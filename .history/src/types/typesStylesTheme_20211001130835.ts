export interface stylesThemeFonts {
  theme: {
    font: {
      family: {
        RobotoMono: string;
        Poppins: string;
        Effect: string;
      };
    };
  };
}

export interface stylesThemeColors {
  theme: {
    colors: {
      white800: string;
      white900: string;
      black900: string;
      black800: string;
      black700: string;
      red700: string;
    };
  };
}
