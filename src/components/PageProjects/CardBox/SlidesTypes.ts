type buttonProps = {
  __typename?: any;
  url: string;
  label: string;
};
export type imageProps = {
  url: string;
  title: string;
  subDescription: string;
  alt: string;
  buttons: buttonProps[];
};
