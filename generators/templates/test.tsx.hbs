import { render } from "@testing-library/react"; import
{{pascalCase name}}; from "."; import Teste from "."; describe("{{pascalCase
  name
}}", () => { it("should render ", () => { const { container } = render({{pascalCase
  name
}}); expect(container).toMatchSnapshot(); }); });
